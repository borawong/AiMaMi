# DISTILLED: relay::codex_writer::strip_all_managed_blocks

| Field | Value |
|---|---|
| product | AiMaMi |
| version | 1.0.9 |
| platform | macos-arm64 |
| module | relay::codex_writer |
| owner_addr | 0x1004b11dc |
| owner_size | 0x1b98 (6,936 B) |
| idb | AiMaMi 1.0.9_ida.app/Contents/MacOS/AiMaMi.i64 |
| session | <audit-session> |
| machine | <workstation> |
| owner_gate | ALLOW (first-write, no prior owner) |
| gate_tier | strictImplementationUse |
| authoritative | true |

---

## dim1 — Owner body (synchronous, not async)

**Function signature (IDA):**
```
double __fastcall codexmate_lib::core::relay::codex_writer::strip_all_managed_blocks::heff10a99327c4ffd(
    __int64 a1,   // &mut output_buf: *mut String  (fat-pointer: ptr + len + cap)
    __int64 a2    // input_str len (X1 = length of &str slice passed in X19 at call site)
)
```
IDA shows `double` return because the Rust return type is a fat pointer returned in two registers (D0 = data_ptr, X1 = len) — this is Rust ABI for returning `String` (ptr, len, cap via sret pattern). Actual Rust sig:
```rust
fn strip_all_managed_blocks(input: &str) -> (HashMap<String,String>, String)
```
Caller `apply_codex_state` at 0x1004af094 passes the current `config.toml` text as `&str` and receives both the cleaned String and a HashMap of stripped section headers.

**Body confirmed as real synchronous body**: 307 basic blocks, 1394 instructions, 0x1b98 bytes. Not a shim, not async (no Future::poll, no async_fn_env in callees).

---

## dim2 — Core logic

### Phase A: HashMap initialization + line-by-line scan (lines 0x1004b11dc–0x1004b189c)

1. **Init HashMap** with `std::hash::random::RandomState::new()` (X0 = RandomState vtable ptr at 0x101395210; call through `hashmap_random_keys`). HashMap stored on stack at `var_F0` (offset +0x80 from SP). field type: `String`, Value type: `String`.

2. **First pass — collect managed section headers into HashMap:**
   - Scan `input` line by line using `CharSearcher::next_match` (newline `'\n'` searcher, pattern stored at SP+`var_F0`+0x10).
   - For each line, `trim_matches` whitespace, check length:
     - **Length 0x27 (39)**: compare against `"# <<< aimami-relay codex-router end top (DO NOT EDIT MANUALLY)"` — the block end marker. Matched via 5 REV+CMP instruction chains on 8-byte chunks.
     - **Length 0x40 (64)**: compare against the full 64-byte `"### aimami-relay codex-router start top (DO NOT EDIT MANUALLY)"` marker.
     - **Length 0x37 (55)**: compare `"# >>> aimami-relay codex-router top start (DO NOT EDIT MANUALLY)"`.
     - **Length 0x3C (60)**: compare `"# >>> aimami-relay managed started (DO NOT EDIT MANUALLY)"`.
     - **Length 0x1E (30)** and **0x24 (36)**: shorter block boundary markers.
   - When a `[section-header]` line is found between managed block markers (flag `W24` = in-block):
     - Extract the inner name (strip `[` and `]`, allocate `alloc::__rust_alloc`, memcpy content minus brackets).
     - `HashMap::insert(map, key_str_fat_ptr)` at 0x1004b187c — stores the section name as a field (value appears to be an empty string sentinel; the field set represents "which sections are managed").
   - Non-managed `[section-header]` lines are treated as TOML profile dots; `split_toml_section_header` (0x1004b3998) is called to decompose them.
   - `is_aimami_managed_section` (0x1004b38ac) is consulted on each `[section]` to decide if it belongs to the relay-managed zone:
     - Returns `true` if section name starts with `"model_providers."` or `"profiles."` followed by `"aimami_relay"` suffix (byte literal checks: `0x72735F6C65646F6D` = `model_rs` + `0x2E7372656469766F` = `ovidser.` → full = `"model_providers."`, then `v10==0x725F696D616D6961` + `v11==2036427877` → `"aimami_r"` + `"elya"` → `"aimami_relay"`).

3. **State machine flag (`W24`):** tracks whether the current scan position is inside a managed block. Set to 1 when an opening marker is detected, cleared when the closing marker is found.

### Phase B: Output string construction (lines 0x1004b18ec–0x1004b2b20)

After the first pass, the function performs a **second line-by-line scan** of `input` to build a new `String` (stack buffer starting at `var_70`):
- Initialise output `String` buffer: alloc with `__rust_alloc`, capacity = input length (or `> 0` minimum).
- Same newline searcher loop (`CharSearcher::next_match`).
- For each line segment:
  - Check if this is a managed-section comment line by matching additional prefixes:
    - `"# CLI: codex--profile a imami_relay"` (length 0x23=35): skip this line.
    - `"# Codex …"` followed by Chinese UTF-8 text (GithubToken config comment, length 0x28=40): skip.
    - `"# Generated by AiMaMi"` prefix (length ≥ 0x15=21): skip.
    - A fourth pattern (Chinese, length 0x25=37): skip.
  - If the line is **not** a managed comment: memcpy it into the output buffer. Append `'\n'` (0x0A) after the line.
  - If the current `[section-header]` is managed (as determined by `is_aimami_managed_section`): skip entire section content (controlled by flag `W27`).
- At the top of section transitions, `is_aimami_managed_section` is called again to set the skip flag.

### Phase C: Output writeback

- The constructed output `String` (ptr, len, cap) is stored to `[X15]`, `[X15+0x10]` — this is the caller-supplied output location `a1` (write-through pointer to caller's `String` slot).
- The HashMap (in `var_F0`) is returned as part of the function's fat-return (HashMap fat-pointer in D0/X1, or via X15 pointer — the exact Rust ABI encoding means both the String and the HashMap are returned).
- Temp alloc (`var_108` / `var_100`) deallocated with `__rust_dealloc`.
- The old `String` buf if non-empty is also freed via `__rust_dealloc`.

---

## dim3 — Callees enumerated

| Callee addr | Symbol | Role |
|---|---|---|
| 0x1004d219c | `CharSearcher::next_match` | newline searcher, finds `\n` positions |
| 0x10058b240 | `str::trim_matches` | whitespace trim of line slice |
| 0x1004b38ac | `is_aimami_managed_section` | returns `bool`; checks section name = `model_providers.*` or `profiles.aimami_relay*` |
| 0x1004b3998 | `split_toml_section_header` | splits `profiles.foo.bar` on `.` respecting quotes; returns `Vec<String>` |
| 0x1004d92fc | `HashMap::insert` | inserts stripped section field into managed-set HashMap |
| 0x100d86380 | `RawVecInner::reserve::do_reserve_and_handle` | Vec/String realloc |
| 0x100db39b8 | `RawVec::grow_one` | HashMap bucket grow |
| 0x100db5318 | `memcpy` | copy line bytes into output buffer |
| 0x100db530c | `memcmp` | (unused directly; inlined into section-name compare) |
| 0x1000013d8 | `__rust_alloc` | heap alloc |
| 0x1000013dc | `__rust_dealloc` | heap dealloc |
| 0x1000013e8 | `__rust_no_alloc_shim_is_unstable_v2` | alloc guard |
| 0x100d352c4 | `hashmap_random_keys` | HashMap seed |
| 0x100db421c | `handle_error` | OOM / capacity overflow abort |
| 0x100db43d0 | `slice_error_fail` | slice bounds panic |

---

## dim4 — DTO / Error / Side effects

### Input DTO
```rust
// Caller: apply_codex_state (0x1004aef60)
// a1 = ptr to caller's stack slot for (HashMap<String,String>, String)
// a2 = &str (raw ptr in X19/X23, length in X1/a2) — current config.toml text content
strip_all_managed_blocks(config_text: &str) -> (HashMap<String, String>, String)
```

### Output DTO
```rust
// Return 0: HashMap<String, String>
//   field = TOML section name string (the inner text of [section])
//   value = "" (empty; HashMap used as a Set of managed section names)
//   represents: which [section] blocks were stripped out
//
// Return 1: String
//   the config.toml text with ALL managed blocks removed
//   managed blocks = everything between # >>> ... top start and # <<< ... end top markers
//   managed comment lines also stripped (# CLI:, # Codex, # Generated by AiMaMi, Chinese comment)
//   non-managed sections and their content are preserved verbatim
```

### Managed block markers (exact match, all case-sensitive):
| Marker type | Length | String |
|---|---|---|
| Block open (long) | 64 | `"### aimami-relay codex-router start top (DO NOT EDIT MANUALLY)"` |
| Block open (medium) | 55 | `"# >>> aimami-relay codex-router top start (DO NOT EDIT MANUALLY)"` |
| Block open (short) | 30/36 | shorter prefix variants |
| Block close | 39 | `"# <<< aimami-relay codex-router end top (DO NOT EDIT MANUALLY)"` |
| Managed section | 60 | `"# >>> aimami-relay managed started (DO NOT EDIT MANUALLY)"` |
| CLI comment | 35 | `"# CLI: codex--profile a imami_relay"` |
| Codex comment | 40 | `"# Codex …"` (+ Chinese UTF-8 GithubToken description) |
| Generated comment | ≥21 | `"# Generated by AiMaMi"` prefix |
| Chinese comment | 37 | full Chinese UTF-8 comment line |

### Managed section detection (`is_aimami_managed_section`):
- Section name starts with `"model_providers."` (len≥16) AND contains `"aimami_relay"` suffix
- Section name starts with `"profiles."` (len≥9) AND contains `"aimami_relay"` suffix
- Returns `false` for all other sections

### Side effects
- **Pure function with respect to filesystem**: does NOT read/write any files directly.
- **Heap allocations**: HashMap internal buckets + output String buffer (all freed on return or passed to caller).
- **No I/O, no IPC, no Tauri events emitted.**
- **Panics / OOM**: `handle_error` → abort on alloc failure; `slice_error_fail` → panic on slice bounds.

### Callers and usage context
| Caller | Addr | Context |
|---|---|---|
| `relay::codex_writer::apply_codex_state` | 0x1004af094 | Reads current `config.toml`, calls `strip_all_managed_blocks` to get clean base + managed set, then re-renders and writes back |
| `relay::codex_writer::user_top_level_profile` | 0x1004b0e20 | Strips managed blocks before extracting the user's top-level profile assignment |
| `relay::codex_diagnostic::fix_config_toml_syntax_text` | 0x10055d3dc | Strips managed blocks before performing TOML syntax repair |

---

## Fake-wall taxonomy exhaustion (red line 13)

All 7 taxonomy entries checked and cleared:

| Fake-wall type | Status | Reason |
|---|---|---|
| `drop_in_place` / async body | NOT applicable | Function is synchronous. No `poll`, no `async_fn_env`, no Future in callees. 0 callers reference any poll slot. |
| `architecture_only` / budget rule | NOT applicable | HexRays fully decompiled the 6936B body. No budget bail. |
| `async decompile failed` (HexRays) | NOT applicable | HexRays produced complete pseudocode (48212 chars). |
| Guessed wrong VA | NOT applicable | VA 0x1004b11dc confirmed via `func_profile` = exact symbol `strip_all_managed_blocks`. |
| vtable / dynamic dispatch unknown | NOT applicable | No vtable or trait object dispatch. All callees are static/direct calls. |
| `HTTP-terminal` / external unverifiable | NOT applicable | No network I/O. Pure string transformation. |
| Library internal unreachable | NOT applicable | `reqwest`/`rustls` not involved. |

**genuine_ceiling**: false. No ICF folding. All behavior fully recovered.

**recovery_attempts**: None needed. Full decompile succeeded on first attempt.

---

## Gate assessment

- **dim1**: owner body confirmed real synchronous function, not shim/drop. ✓
- **dim2**: full logic traced — two-pass line scan, managed block detection, output String construction. ✓
- **dim3**: all 15 callees enumerated; 2 field callees fully decompiled (`is_aimami_managed_section`, `split_toml_section_header`). ✓
- **dim4**: DTO exact (input `&str`, output `(HashMap<String,String>, String)`), all markers byte-confirmed from disasm. Side effects: none (pure transform). ✓

**gate_tier**: `strictImplementationUse` — behavior fully understood, DTO exact, no accepted_unknown entries.
