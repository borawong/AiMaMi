# macOS accounts raw rules

Review this raw scope against current AiMaMi 1.0.9 macOS IDA evidence only.
Validate the seven-file raw template, manifest hashes, gate fields, INDEX rows,
and IDB save state before accepting full_leaf_100.
