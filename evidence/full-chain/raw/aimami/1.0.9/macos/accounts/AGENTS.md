# macOS accounts raw rules

This directory contains canonical AiMaMi 1.0.9 macOS accounts raw leaves.
Backend proof must use the matching macOS IDB and current same-platform IDA
owner/callee/DTO/side-effect evidence. Each command leaf keeps the raw template:
AI.md, README.md, SYSTEM-DIFF.md, audits/*-ida-closure.json,
validation/result.json, file-manifest.json, manifest.json.
