# request_accessibility_permission

AiMaMi 1.0.9 macos voice command closure.

IDA MCP searched the matching macos IDB for this command and for the complete 34-command voice batch. The command string is absent, so no Tauri backend owner, wrapper body, callee chain, DTO response builder, error envelope, or side-effect implementation leaf exists in this binary.

Accepted substitute: same-platform backend absence. The only voice-related binary evidence is path/UI/capability anchoring: 0x100f3933a voice-workspace.json/voice-runtime.json path anchor, 0x100f35807 voice-overlay UI anchor, 0x100f38815 voice-search-overlay UI anchor, 0x100edde07 global-shortcut plugin constant.

Consumer implication: do not create upstream-parity backend behavior for this command. If upstream restores voice, that is Product decision work, not AiMaMi 1.0.9 backend parity.
