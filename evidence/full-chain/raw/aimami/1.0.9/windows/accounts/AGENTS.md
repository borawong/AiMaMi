# AGENTS.md — raw windows accounts

This directory is a canonical AiMaMi 1.0.9 Windows accounts reverse-evidence scope. Backend proof must use IDA MCP against the matching Windows IDB only. Use current Windows IDA proof only. One command has one canonical raw leaf. PREWRITE_PRODUCER_COLLISION_GATE_V1 applies before every write.
