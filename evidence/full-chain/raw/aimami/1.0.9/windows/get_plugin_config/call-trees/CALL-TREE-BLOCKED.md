# Call Tree Status

Leaf: `get_plugin_config`

No accepted Windows 1.0.9 call-tree for this leaf is included in this bundle.

Current PE command-table evidence only proves the command string surface. It does not prove handler ownership, service/core calls, persistence, response shaping, or error return behavior.

Promotion requires a same-platform call-tree with at least accepted implementation leaf termination or an accepted substitute under the current gate spec.