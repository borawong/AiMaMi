import type { CustomInstructionsStoreState } from './types';

export const customInstructionsInitialState: CustomInstructionsStoreState = {
  moduleId: 'custom-instructions',
  restorationTier: 'P2',
};
