# 模块 locale 迁移约定

本目录承接从集中式 `zh.json` / `en.json` 迁移出来的模块文案。当前集中 JSON 仍作为资源基座，模块迁移只能逐个登记到 `index.ts`，避免一次性拆散导致资源漂移。

- 每个模块文件只拥有本模块 key，不能借用其他模块 key。
- 每个模块必须同时提供 `zh` 和 `en`，叶子 key 结构必须一致。
- 迁移时先从集中 JSON 移入一个小模块，再把模块加入 `localeModules`。
- 不确定来源的文案只保留待补证据说明，不写成真实用户可见文案。
